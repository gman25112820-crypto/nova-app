Burrow entrance + doorway shipped to prod.

Commits: 5bbac3b (Safe Corner tile rewire — house tile 10 now opens the real
SafeCornerLivingRoom instead of an orphaned empty RoomDetailScreen), d3d8a78
(Burrow tile + underground entrance placeholder), 1657963 (dig invitation +
arch clamp + back-button align).

Doorway tap is a deliberate silent no-op — a comment in
underground_entrance_screen.dart marks where the real dig flow wires in.

Known cosmetic note: the back-button fix was an explicit Align(topLeft) laid
over a centring mechanism neither of us could identify from the code — the
trace said it should already render top-left, but it didn't live. If it ever
recurs, go straight to DevTools inspection, not another round of code-reading.

Next session: dig flow design — finalise the four room-feel names, five lamp
colours, and the Pattern B storage schema (nested list in the child's own
JSON, per docs/session_notes/DESIGN_2026-07-13_underground.md).
